# 내루미 DOM 분리 계획 (naeru.me)

> 작성 2026-09-01. 이 문서는 **실행 지시서**다. 작업 전에 `CLAUDE.md`와
> `tools/camp-overlay/{build.sh,punch.py}`를 먼저 읽을 것.

---

## 0. BLUF — 이것만 읽어도 전체가 파악되게

**무엇을:** 배경 영상에 통째로 구워져 있는 내루미를 떼어내 DOM 요소로 만든다.

**어떻게:** 배경 영상 16개는 **손대지 않는다.** 대신
1. 내루미가 서 있던 자리를 깨끗한 초원으로 덮는 **정지 패치 PNG**(변형별 8장)를
   배경 위에 깔고,
2. 그 위에 **알파 채널이 있는 내루미 영상**(변형별 webm/mp4)을 DOM으로 얹는다.

**왜 이 방식:**
- 배경 영상을 만든 원본 파이프라인(업스케일·구름막·그레이딩) 스크립트가 레포에
  **없다**(`tools/camp-overlay`만 남아 있음). 배경 재생성에 의존하면 막힌다.
- 배경 16개를 다시 구우면 git이 **+108MB**, 되돌리기도 어렵다. 패치 방식은
  새 파일만 추가하므로 `index.html` 한 줄로 껐다 켤 수 있다.
- 내루미 주변 초원은 루프 내내 거의 안 움직인다(측정치 §1) → 정지 패치를 깔아도
  눈에 띄지 않는다.

**완료 기준(Phase 0):** 화면이 **지금과 픽셀 단위로 같아야 한다.**
합성 복원 PSNR ≥ 40dB + 6개 화면비 브라우저 픽셀 diff.

**단계:**

| 단계 | 내용 | 이 문서 |
|---|---|---|
| **Phase 0** | 겉보기 동일한 분리 (패치 + 알파 클립 + 레이어 재배치) | §3 전체 명세 |
| **Phase 1** | 제자리 반응 — **폴짝 · 두리번 · 좌우 뒤집기 · 움찔** ← 여기까지가 목표 | §4 전체 명세 |
| Phase 2 | 진짜 이동 — 그림자 분리·원근 스케일·새 포즈 | §5, **보류** |

**작업 순서 요약**
1. `tools/naeru-split/plate.py` — 변형별 플레이트 패치 8장 생성
2. `tools/naeru-split/matte.py` — 프레임별 매트 + 언프리멀티플라이 색 → PNG 시퀀스
3. `tools/naeru-split/build.sh` — 위 둘을 호출하고 알파 영상 16개 + 정지 8장 인코딩
4. `index.html` — 레이어 2쌍 추가(plate A/B, naeru A/B), 크로스페이드에 연결
5. `tools/naeru-split/verify.py` + 브라우저 diff로 검증 — **여기까지가 Phase 0**
6. 행동 IIFE 추가 — 폴짝·두리번·뒤집기·움찔 (§4)
7. `CLAUDE.md` §2 규칙 갱신 (지금 규칙은 이 변경의 정반대다)

**하지 말 것:** `img/meadow-*.mp4`·`img/sky-*.jpg` 수정, `camp-overlay/build.sh`
실행, 검증 전 커밋(**커밋 = Netlify 배포**).

---

## 1. 이미 측정한 사실 — 다시 재지 말 것

| 사실 | 수치 | 계획에서의 의미 |
|---|---|---|
| 8변형은 픽셀 정렬돼 있다 | 엣지맵 차이 offset(0,0)=2.20 < (1,0)=3.25 < (2,0)=4.03 | **매트를 1벌만 만들어 8변형에 그대로 적용** |
| 내루미 주변 초원은 안 움직인다 | 프레임간 mean 0.28 (앞쪽 풀 2.18, 내루미 몸 1.44) | 정지 패치가 보이지 않는다 |
| 루프 반주기 드리프트도 작다 | \|f0−f158\| mean 1.38, p99 6 (내루미 몸은 mean 8.45, p99 89) | 6.6초 동안 패치 경계가 어긋나지 않는다 |
| 실루엣이 루프 동안 흔들린다 | union−intersection = union의 45%, 4K에서 약 10~25px | **단일 마스크 불가 → 프레임별 매트 필수** |
| 분홍기 키 단독으로는 부족 | 배 크림색 줄무늬가 구멍으로 남음 (`R−max(G,B)`가 낮음) | 홀필(flood fill) 단계 필요 |
| 인코더 | `libvpx-vp9`(yuva420p), `hevc_videotoolbox`(`-alpha_quality`) 둘 다 있음 | 알파 영상 2종 인코딩 가능 |
| 파이썬 환경 | **PIL 12.2.0 있음, numpy 없음** | 순수 PIL 밴드 연산(`ImageChops`, `point`)으로 짤 것. 4K를 파이썬 루프로 돌면 수십 초씩 먹는다 |
| 저장소 크기 | `.git` 355MB, `img` 108MB | 배경 재인코딩은 최후의 수단 |

재현 명령은 §8 부록 B.

---

## 2. 확정 사항과 미결 사항

**확정 (질문 없이 이대로 진행)**
- 배경 영상·정지 프레임은 **불변**. 지우기는 DOM 패치 레이어로.
- 내루미 에셋은 **현재 영상에서 316프레임 그대로 롤로스코핑**. 새로 그리거나
  픽셀 스프라이트로 대체하지 않는다 (회화풍 배경과 스타일이 어긋난다).
- 316프레임 핑퐁 구조 유지. 프레임 수를 바꾸면 루프가 튄다.
- **접지 그림자는 배경에 남겨둔다.** 내루미는 제자리에 서 있고, 폴짝은 수직이라
  그림자와 수평 정렬이 유지된다. 만화 문법상 뜬 캐릭터의 그림자를 그대로 두는 건
  정석 표현이다. 대신 그림자가 동작 진폭의 상한을 정한다(§4.1).
- **목표는 Phase 1까지.** 제자리에서 폴짝·두리번·좌우 뒤집기·움찔. 새 아트가
  필요 없는 범위다. 진짜 보행(Phase 2)은 여기까지 해보고 판단한다.

**미결 (Phase 0을 막지는 않음)**
- Phase 0 확정 후 패치를 영상에 구워 넣을지(§7 부록 A). 런타임 레이어 2장이
  줄지만 git +108MB.

---

## 3. Phase 0 — 겉보기 동일한 분리

### 3.1 만들 파일

```
tools/naeru-split/
  plate.py     변형별 플레이트 패치 PNG 생성
  matte.py     프레임별 알파 + 언프리멀티플라이 색 → PNG 시퀀스
  verify.py    plate ⊕ naeru == 원본 인지 PSNR로 검증
  build.sh     위를 순서대로 부르고 인코딩까지 (한 방 실행)
  build/       중간 산출물 — .gitignore에 추가할 것

img/
  plate-<변형>.png        3840×2160 RGBA, 패치 rect만 불투명 (≈300KB × 8)
  naeru-<변형>.webm       VP9 알파 (Chrome/Edge/Firefox)
  naeru-<변형>.mp4        HEVC 알파 (Safari)
  naeru-<변형>.png        첫 프레임 정지본 (reduced-motion·폴백)
  alpha-probe.webm        16×16 1프레임 완전 투명 (알파 지원 판정용, ~1KB)
```

`<변형>` = `{dawn,day,dusk,night} × {"", -rain}` 8종. **기존 명명 규칙과 동일하게
유지할 것** — JS가 문자열로 조립한다.

예상 증가분 **+5~10MB**. 초과하면 매트 crf를 올리기 전에 크롭을 줄여라.

### 3.2 크롭 상자 = 이 작업의 유일한 계약

내루미 스프라이트의 크롭 사각형은 `matte.py`와 `index.html` **양쪽에 적힌다.**
어긋나면 **에러 없이 몇 px 밀린 채로 그려진다.** 값을 바꾸면 반드시 양쪽을 대조.

초안값 (union 실루엣 + 여유 32px, 16의 배수로 정렬):

```
CROP = x 1790, y 1330, w 480, h 464          # 3840×2160 프레임 좌표
→ CSS %  left 46.6146%  top 61.5741%  width 12.5%  height 21.4815%
```

`plate.py`가 실제 union bbox를 계산해 출력한다. 출력값이 위 상자 안에 **여유
16px 이상**을 두고 들어가는지 확인하고, 아니면 상자를 키우고 양쪽을 같이 고쳐라.

참고 좌표 (CLAUDE.md §2에서): 내루미 x1866–2174, y1377–1720(발끝),
머리 꼭대기 (1995, 1399) = 프레임 비율 (0.520, 0.648).

### 3.3 `plate.py` — 내루미를 지운 초원

입력: 각 변형 mp4의 첫 프레임 PNG (`ffmpeg -i img/meadow-<v>.mp4 -frames:v 1`).

1. **union 실루엣**: dusk 영상 316프레임 전부에 분홍기 키
   (`R − max(G,B) > 32`, `punch.py`와 같은 임계, y 1370~1780 / x 1790~2270 안에서만)
   → 프레임별 마스크의 합집합 → 닫기(Max9→Min9)로 잡티 정리 → **24px 팽창**.
2. **접지선 아래 페이드아웃**: 발끝(y≈1720) 부근부터 마스크를 0으로 떨어뜨린다.
   `punch.py`의 `WATER_Y` 페이드와 같은 방식. **그림자를 지우지 않기 위한 것**이니
   반드시 넣을 것.
3. **채우기**: 마스크 안쪽을 같은 프레임의 깨끗한 초원에서 클론한다. 수평으로
   ±450px 옮긴 조각을 쓰고(같은 y 밴드를 유지해야 언덕–초원 경계선이 맞는다),
   좌/우 두 소스를 마스크 가장자리 쪽으로 페더 블렌드.
   - 왼쪽 소스는 x1670 부근에 작은 나무가 있으니 복제되지 않는지 눈으로 확인.
   - 클론 결과가 어색하면 그때 대안: Photoshop 제너레이티브 필 또는
     iopaint/LaMa(venv 필요). 8장을 손으로 만들어도 되는 규모다.
4. 결과를 3840×2160 RGBA로 저장. **패치 rect 밖은 완전 투명.**
   투명 영역이 대부분이라 PNG가 잘 압축된다.

> 변형별로 **각자의 첫 프레임에서** 클론하므로 색이 자동으로 맞는다.
> CLAUDE.md §4의 밴드별 그레이딩 체인을 재현할 필요가 **없다.**

### 3.4 `matte.py` — 내루미 뽑아내기

마스크는 **dusk 한 벌만** 계산해서 8변형에 그대로 쓴다(§1 정렬 확인됨).

프레임 n마다:
1. 분홍기 점수 `p = R − max(G,B)`를 **소프트 램프**로 알파화:
   `a = clamp((p − 22) / (40 − 22))` → 하드 임계와 달리 가장자리가 안티에일리어싱된다.
2. 닫기(Max→Min)로 잡티 정리 후, **크롭 바깥에서 flood fill** → 채워지지 않은
   내부 영역(배 크림색 줄무늬)을 실루엣에 편입. 이 단계 없으면 배에 구멍이 뚫린다.
3. 0.8~1.5px 가우시안 블러로 경계 정리.
4. **색은 언프리멀티플라이로 구한다:**
   `C = (O − (1−a)·P) / a`  (= `P + (O−P)/a`, O=원본 프레임, P=플레이트 패치)
   - 배경 스필이 빠지고, **P 위에 다시 얹으면 O가 그대로 복원된다.**
   - `a < 0.15`인 화소는 나눗셈이 노이즈를 증폭하므로 클램프 후 그대로 둔다.
     문제가 되면 §3.8의 PSNR 리포트에 잡힌다.
5. 크롭(§3.2)해서 `build/naeru-<변형>/%04d.png`로 저장. 316프레임 전부.

### 3.5 인코딩 (`build.sh`)

```sh
# VP9 알파 — Chrome/Edge/Firefox
ffmpeg -framerate 24 -i $B/naeru-$V/%04d.png \
  -c:v libvpx-vp9 -pix_fmt yuva420p -crf 26 -b:v 0 \
  -auto-alt-ref 0 -row-mt 1 -deadline good -cpu-used 2 \
  -y img/naeru-$V.webm

# HEVC 알파 — Safari (bgra 입력 + alpha_quality가 핵심, hvc1 태그 필수)
ffmpeg -framerate 24 -i $B/naeru-$V/%04d.png \
  -c:v hevc_videotoolbox -pix_fmt bgra -alpha_quality 0.9 -q:v 65 \
  -tag:v hvc1 -movflags +faststart \
  -y img/naeru-$V.mp4
#   -q:v가 거부되면 -b:v 2500k 로 대체

# 정지본
cp $B/naeru-$V/0001.png img/naeru-$V.png     # 또는 pngquant로 축소
```

- `-auto-alt-ref 0`: VP9 알파에서 alt-ref가 알파 평면을 깨뜨린 사례가 있다.
- **HEVC 알파는 ffmpeg으로 되감아 검증할 수 없다**(디코더가 알파 레이어를 안 내놓음).
  진짜 검증은 Safari 실기뿐이다(§3.8-3).

### 3.6 `index.html` 변경

**레이어 순서** (아래 → 위). 굵은 것이 신규.

```
.bg-still                 ┐
video.bg A/B              ├ --bg-shake 그룹
.sky-freeze A/B           │
**.plate A/B**            ┘  ← 배경과 같은 cover/center. 좌표 계산 없음
**#stage → #naeru A/B**      ← --bg-shake 그룹에 포함(구워져 있을 때처럼 같이 흔들림)
.rain ×2
.vignette
#frame → #ball #bonk
.clock-wrap
```

`#stage`를 `.rain`·`.vignette` **아래**에 두는 게 중요하다. 구워져 있던 내루미
위로 비와 비네트가 얹혀 있었으므로, 그래야 지금과 톤이 같다.

**CSS**

```css
/* ── 플레이트 패치: 영상에 구워진 내루미를 지우는 정지 조각.
   .sky-freeze와 같은 이유로 A/B 두 장을 겹쳐 크로스페이드한다.
   배경과 똑같이 cover/center라서 화면비가 뭐든 픽셀이 맞는다 ── */
.plate { position: fixed; inset: 0; pointer-events: none;
         background-size: cover; background-position: center;
         opacity: 0; transition: opacity 2.5s ease; }
.plate.on { opacity: 1; }
#plateA { background-image: var(--plate-a); }
#plateB { background-image: var(--plate-b); }

/* 셰이크 그룹에 plate와 stage를 추가 — 배경과 어긋나면 패치 경계가 드러난다.
   #stage는 프레임 사각형과 중심이 같으므로 scale도 정확히 같이 먹는다 */
.bg-still, video.bg, .sky-freeze, .plate, #stage {
  transform: var(--bg-shake, none);
}

/* ── 내루미: #stage가 프레임 사각형(=배경 영상이 cover로 차지하는 상자)이라
   자식은 % 좌표만으로 배치된다. JS 기하 계산 없음 ── */
/* z-index를 주지 말 것 — .rain/.vignette와 같은 레벨이라 DOM 순서로 결정된다.
   마크업에서 반드시 .rain 앞에 넣는다 */
#stage { position: fixed; pointer-events: none; }

/* transform은 속성이 하나뿐이라 두 코드가 같은 요소에 쓰면 서로 덮어쓴다.
   그래서 역할별로 겹을 나눠 각자 자기 transform만 소유하게 한다.
   Phase 0에선 셋 다 transform이 없지만, 지금 만들어두면 Phase 1에서
   마크업을 다시 안 건드린다 */
#naeru-move {                      /* 폴짝·이동 (translate) */
  position: absolute;
  left: 46.6146%; top: 61.5741%; width: 12.5%; height: 21.4815%;
}
#naeru-act, #naeru-face { position: absolute; inset: 0; }
/* 회전·스쿼시의 기준점은 발끝이다. 크롭 상단이 y1330, 발끝이 y1720이므로
   세로 (1720-1330)/464 = 84%, 가로는 몸 중심 x2020 → (2020-1790)/480 = 47.9%.
   이 값이 틀리면 기울일 때 발이 지면에서 뜬다 */
#naeru-act, #naeru-face { transform-origin: 47.9% 84%; }
.naeru, #naeruStill {
  position: absolute; inset: 0; width: 100%; height: 100%;
  opacity: 0; transition: opacity 2.5s ease;
}
.naeru.on, #naeruStill.on { opacity: 1; }
```

**마크업**

```html
<div id="stage">
  <div id="naeru-move"><div id="naeru-act"><div id="naeru-face">
    <video id="naeruA" class="naeru" muted loop playsinline preload="auto"></video>
    <video id="naeruB" class="naeru" muted loop playsinline preload="none"></video>
    <img   id="naeruStill" alt="">
  </div></div></div>
</div>
```

**JS**
- `layout()`(현재 공 IIFE 안)을 프레임 사각형을 계산하는 공용 함수로 올려서
  `#frame`과 `#stage` **둘 다**에 적용. 수식은 그대로.
- `show()` / `crossfadeTo()`에 `plates[slot]`, `naerus[slot]` 배열을 추가해
  기존 A/B 슬롯 전환에 그대로 태운다. 배경 영상과 **같은 2.5s**로.
  - `--plate-a/--plate-b`는 `--freeze-a/--freeze-b`와 완전히 같은 패턴.
  - 내루미 영상은 배경과 시간을 맞출 필요가 **없다**(더 이상 같은 그림이 아니다).
    새 슬롯은 `currentTime = 0`으로 시작해도 된다.
- `noMotion`(prefers-reduced-motion) 경로에서도 **내루미가 보여야 한다.**
  영상 대신 `#naeruStill`의 `src`를 변형에 맞춰 교체.
- **알파 지원 판정** (`img/alpha-probe.webm` 사용):

```js
/* Safari는 VP9 webm을 재생은 하되 알파를 무시해 검은 사각형으로 그린다.
   canPlayType으로는 구분이 안 되므로, 16×16 완전 투명 프로브를 캔버스에
   그려 실제 알파를 읽는다. 본편을 받기 전에 판정해야 검은 사각형이 안 뜬다 */
```
   판정: 프로브의 코너 알파가 0이면 `.webm`, **알파가 불투명하게 나오거나
   프로브 자체를 못 읽으면** `.mp4`(HEVC). 그 mp4마저 `error`를 내면
   `#naeruStill`로 후퇴. 기존 배경 코드의 HEVC 폴백과 같은 태도.
- 디버그 파라미터 `?still=1` 추가 — `noMotion` 경로를 강제한다. §3.8-2의
  브라우저 픽셀 diff가 이걸 쓴다. `?v=` `?h=` `?w=` `?ball=`은 그대로 유지.
- 기능은 독립 IIFE로. 내루미 레이어가 터져도 시계·배경은 살아야 한다.

### 3.7 모바일에서 어색하지 않기 위한 조건 (필수)

DOM으로 바꿔도 배경과 **같은 비율로 같이 잘리고 같이 커져야** 한다. 그게
보장되는 이유와, 추가로 지켜야 할 것:

- **패치**는 `.bg-still`과 글자 그대로 같은 CSS(`cover`/`center`)다. 좌표 계산이
  아예 없으므로 화면비가 뭐든 배경과 픽셀이 어긋날 수 없다.
- **내루미**는 `#stage`(= 배경 영상이 cover로 차지하는 사각형) 안에서 **% 좌표만**
  쓴다. 개체마다 cover 수식을 태우지 않는다 — 공(`#frame`)이 이미 6개 화면비에서
  검증한 방식이다.
- **세로 모바일 가시 대역**: 프레임 가로 37~63%(x1421~2419)만 보인다.
  내루미 크롭은 x1790~2270 = **46.6~59.1%** → 여유를 두고 안쪽이다. 세로에서도
  온전히 보인다. 세로는 잘리지 않는다.
- **해상도**: 4K 프레임을 화면 폭에 맞춰 늘리는 배율과, 480px 크롭을 `#stage`
  폭의 12.5%로 늘리는 배율은 **정확히 같다**. 3배 DPR 폰에서 둘 다 약 1.17배
  업스케일 → 내루미만 흐려지거나 또렷해지는 일이 없다.
- **`poster` 필수**: 내루미 영상 2장에 `poster="img/naeru-<변형>.png"`를 붙인다.
  iOS 저전력 모드는 `play()`를 거부하는데, poster가 없으면 그 자리가 **빈 구멍**이
  된다(배경에서는 이미 지워졌으므로). poster가 있으면 정지 내루미로 자연스럽게
  떨어진다.
- 위 두 가지(가시 대역·poster)는 §3.8-2/3 검증에서 반드시 확인한다.

### 3.8 검증 (전부 통과해야 커밋)

0. **게이트 — plate 단독 / 스프라이트 단독** (아래 1번보다 먼저).
   **복원 PSNR만으로는 분리 실패를 못 잡는다.** 2026-09-01에 실제로 겪었다:
   plate에 캐릭터가 남고 스프라이트 알파에 배경이 딸려 들어간 상태에서도
   복원은 41dB로 통과했다. `C = P + (O−P)/a`라 `P≈O`이면 알파가 뭐든 `O`가
   복원되는 축퇴 해이기 때문이다. 제자리에선 멀쩡하고 **폴짝하는 순간 무너진다.**
   - (A) plate의 지운 자리가 아직 캐릭터 색인가 — 분홍기(R−max(G,B)) 평균이
     주변 초원 링 + 6 이내여야 한다 (초원 p50=16, 내루미 p50=41).
   - (B) 스프라이트가 몸 밖을 달고 다니는가(패치 바깥 평균 알파 ≤ 3) /
     몸 안에 구멍이 났는가(실루엣 20px 침식 안쪽 평균 알파 ≥ 250).
   - 둘 다 `verify.py --pass1`이 자동으로 찍는다. **눈으로도 한 번 본다** —
     plate를 자홍색 배경 위에 얹어 캐릭터가 안 보이는지, 스프라이트도 같은
     방식으로 배경이 안 딸려오는지.
1. **오프라인 복원 테스트** — `verify.py`. 8변형 × 316프레임에 대해
   `plate ⊕ naeru`를 합성하고 원본 영상 프레임과 비교.
   - 1차: PNG 시퀀스로 (매트 로직 자체)
   - 2차: **인코딩된 webm을 디코딩해서** (알파 압축 손실 포함)
   - 기준: 크롭 영역 PSNR **평균 ≥ 40dB, 최악 프레임 ≥ 34dB**. 최악 프레임
     번호와 변형을 출력할 것.
   - numpy가 없으므로 MSE는 `ImageChops.difference` → `histogram()`으로 계산.
2. **브라우저 픽셀 diff** — `python3 -m http.server`로 띄우고
   (file://은 캔버스가 오염돼 알파 프로브가 죽는다),
   `?still=1&v=<변형>&w=<날씨>`로 8변형 × 6화면비
   (16:9 / 16:10 / 4:3 / 21:9 / 9:19.5 / 1:1) 스크린샷.
   현재 배포본(`git stash` 또는 HEAD의 index.html 사본)과 비교.
   - 정지 프레임끼리라 **결정적**이다. 기준: 평균 절대차 < 1.0, p99 < 4.
   - Edge 헤드리스 idiom은 `camp-overlay/build.sh` 2단계 그대로.
     **파일을 쓰고도 프로세스가 안 죽으니 폴링 후 해당 PID만 kill**
     (사용자가 Edge 상시 사용 — 광역 `pkill` 금지).
3. **아이폰 Safari 실기 1회** (자동화 불가, 필수)
   - HEVC 알파가 투명하게 재생되는가 (검은 사각형이면 실패)
   - 크로스페이드 중 동시 디코더 4장(4K 2 + 소형 2)에서 끊기지 않는가
   - 세로 모바일에서 내루미가 화면 안에 있는가 (가로 37~63%만 보인다)
4. `git status`로 `img/meadow-*`·`img/sky-*`가 **수정되지 않았는지** 확인.

### 3.9 문서 갱신

`CLAUDE.md §2 "절대 규칙: 장식은 DOM이 아니라 영상에 굽는다"`는 이번 변경의
정반대다. 다음 취지로 다시 쓸 것:

- 배경 = 내루미 없는 플레이트, 내루미 = DOM. 좌표는 `#frame`/`#stage` 상자
  안에서 프레임 비율로만 쓴다 (개체마다 cover 수식을 태우는 방식은 여전히 금지 —
  2026-07-21 생일 꼬깔모자 revert 사유).
- 새 장식은 `#naeru-face`의 자식으로 붙이면 폴짝·움찔·뒤집기를 따라다닌다.
- 크롭 상자가 `matte.py` ↔ `index.html` 양쪽에 있는 계약이라는 점.
- **transform 겹 구조**: `#stage`=배경 셰이크 / `#naeru-move`=폴짝·이동 /
  `#naeru-act`=움찔·두리번 / `#naeru-face`=방향. 겹을 지키지 않으면 서로 덮어쓴다.
  회전·스쿼시 origin은 발끝 `47.9% 84%`.
- **동작 진폭의 상한은 구워진 접지 그림자가 정한다**(§4.1 표). 수직은 자유롭고
  수평은 ±20px@4K까지.
- *"내루미는 영상에 구워져 있어 움찔할 수 없다 … 맞고도 표정이 그대로인 게 이
  장면의 의도다"* 는 폐기.

---

## 4. Phase 1 — 제자리 반응 (이번 작업의 목표 지점)

새 아트 없이 **롤로스코핑한 한 포즈만으로** 되는 것들만 넣는다. 네 가지:
**폴짝 · 두리번 · 좌우 뒤집기 · 움찔**.

### 4.1 진폭의 상한은 "구워진 그림자"가 정한다

접지 그림자는 배경에 남아 있다(§2 확정). 그래서 몸이 움직이면 그림자가 따라오지
않는다. **이게 모든 동작의 진폭 상한이다.**

| 움직임 | 그림자와의 관계 | 상한 |
|---|---|---|
| **수직**(폴짝) | 수평 정렬이 유지된다 → **가장 안전** | 자기 키의 **15%**(≈70px@4K) |
| 수평(움찔·두리번) | 그림자가 그만큼 뒤처진다 | **±20px@4K**(몸 폭의 6%) |
| 회전 | origin이 발끝이라 접지점은 안 움직인다 | **2.5deg** |
| 좌우 뒤집기 | 그림자 모양은 안 뒤집힌다 | §4.5 참고 — 육안 확인 후 채택 |

만화가 공중에 뜬 캐릭터의 그림자를 그대로 두는 건 정석 표현이라 폴짝은 오히려
자연스럽다. 다만 높이를 키우면 "그림자가 안 따라온다"가 눈에 띄기 시작하므로 15%.

### 4.2 폴짝 (hop)

- `#naeru-move`에 `translateY`. **CSS transform의 %는 자기 요소 높이 기준**이라
  `translateY(-13%)`로 쓰면 화면비·해상도와 무관하게 같은 비율로 뛴다.
- 스쿼시&스트레치는 `#naeru-act`가 담당(origin 발끝):
  1. 웅크림 110ms — `scale(1.06, 0.92)`
  2. 상승 260ms ease-out — `translateY -13%`, `scale(0.97, 1.05)`
  3. 하강 220ms ease-in — 원위치, 스케일 복귀
  4. 착지 90ms — `scale(1.05, 0.94)` → 120ms 복귀
- 가끔(20%) 2연속. 착지 후 60ms 쉬고 한 번 더, 두 번째는 높이 70%.
- 몸이 뜨면 원래 자리에 **패치(깨끗한 초원)**가 드러난다. 이게 정상이고, 분리의
  효과가 처음으로 눈에 보이는 지점이다.

### 4.3 두리번 (look)

에셋이 한 포즈뿐이라 고개만 따로 못 돌린다. **몸 전체를 갸웃**해서 그렇게 읽히게
한다. `#naeru-act`에 회전 + 미세 이동:

- 한쪽으로 `rotate(±2deg) translateX(±0.8%)` 380ms ease-out → 700ms 정지
  → 반대쪽 420ms → 복귀. 총 2초 남짓.
- 회전각을 2.5deg 넘기지 말 것(§4.1). 넘으면 발이 미끄러진 것처럼 보인다.

### 4.4 움찔 (flinch) — 공에 맞을 때

- 현재 공 IIFE의 `hit()`에서 링·셰이크와 **같이** 발동.
- `#naeru-act`에 맞은 방향으로 `translateX(dir*0.9%) rotate(dir*2deg)` +
  `scale(1.04, 0.94)`를 90ms에 최대 → 320ms 스프링 복귀.
- `#stage`가 `--bg-shake` 그룹이므로 **배경 셰이크 + 자체 움찔이 합성**된다.
  맞은 놈이 더 흔들리는 게 맞다. 단 셰이크는 `#stage`, 움찔은 `#naeru-act`로
  겹을 나눠야 서로 안 덮어쓴다.

### 4.5 좌우 뒤집기 (flip)

`#naeru-face`에 `scaleX(-1)`. **주의할 게 둘 있다.**
- 내루미는 비대칭이다(꼬리 오른쪽, 혀 왼쪽, 엉덩이 소용돌이 오른쪽). 뒤집으면
  배경 조명 방향과 음영이 어긋난다.
- 구워진 접지 그림자는 안 뒤집힌다.

그래서 **폴짝의 공중 구간에서만 전환**한다(공중에서 방향을 바꾸는 것처럼 읽힌다).
뒤집힌 상태는 오래 두지 말고 다음 폴짝에서 원복. 그래도 어색하면 **버려도 되는
기능**이다 — `?act=flip`으로 반복 재생해 눈으로 보고 채택 여부를 정할 것.

### 4.6 스케줄러와 공 IIFE 연동

- 공 IIFE와 **같은 패턴의 독립 IIFE** 하나를 새로 만든다.
  유휴 간격: 폴짝 `25~90초`, 두리번 `40~120초`. `document.hidden`이면 건너뛴다.
  `prefers-reduced-motion`이면 전체를 끈다.
- **동시 실행 금지**: 행동 하나가 도는 동안 다른 행동을 시작하지 않는다(`busy`).
- 공과의 계약은 최소한으로. 행동 IIFE가 `window.naeru`에 셋만 공개한다:

```js
window.naeru = {
  busy:    false,          // 공이 날아오는 동안 새 행동을 시작하지 않게
  flinch:  function (dir) {},
  headAt:  function () {}  // 머리 꼭대기의 현재 화면 좌표 {x, y}
};
```
- 공 IIFE는 `toss()` 시작 시 `naeru.busy = true`로 잠그고, `headAt()`으로 목표점을
  잡는다. 머리 꼭대기는 크롭 내 상대좌표 **(42.7%, 14.9%)** —
  프레임 좌표 (1995, 1399)를 크롭(1790,1330,480,464) 기준으로 환산한 값.
- **`window.naeru`가 없으면 지금처럼 상수 `HEAD_U/HEAD_V`로 동작하게 폴백.**
  행동 IIFE가 터져도 공·시계·배경은 살아야 한다(CLAUDE.md 코드 스타일).
- 디버그: `?act=0` 전체 끄기, `?act=hop|look|flip` 해당 동작만 5초 간격 반복.

### 4.7 이 단계에서 함께 정리할 것

- 모자·소품은 `#naeru-face`의 자식(%좌표)으로 붙이면 폴짝·움찔·뒤집기를 전부
  따라간다. 작년 생일 꼬깔모자를 되돌린 이유가 여기서 사라진다.
- CLAUDE.md의 *"내루미는 영상에 구워져 있어 움찔할 수 없다 … 맞고도 표정이
  그대로인 게 이 장면의 의도다"*는 이때 폐기되므로 같이 고친다.

**Phase 1 확인 항목** (Phase 0의 픽셀 diff는 여기선 못 쓴다 — 움직이니까)
1. `?act=hop`·`?act=look`을 6개 화면비에서 눈으로. 특히 세로 모바일에서 폴짝
   최고점이 시계(상단 8vh~26vh)와 겹치지 않는지.
2. 발이 미끄러지지 않는지 — `transform-origin`이 틀리면 여기서 바로 보인다.
3. `?act=flip` 반복 재생 후 채택/폐기 결정(§4.5).
4. `?act=0`과 `prefers-reduced-motion`에서 전부 멈추는지.
5. 행동 IIFE를 통째로 주석 처리해도 공·시계·배경이 정상인지(폴백 확인).

---

## 5. Phase 2 — 진짜 이동 (**보류**, 별도 판단)

제자리를 벗어나 초원 위를 실제로 이동하는 것. **Phase 1로 충분한지 보고 판단한다.**
착수하려면 아래 네 가지가 전부 필요하다.

- **그림자 분리**: `shadow = O / P` (곱하기 레이어)로 뽑아 별도 DOM 요소로.
  이때 `plate.py`의 접지선 페이드아웃을 **제거**하고 그림자까지 지운 플레이트를
  다시 만들어야 한다. 이게 없으면 §4.1의 진폭 상한을 못 넘는다.
- **원근 스케일**: 지면 y에 따라 크기 보정.
- **이동 밴드가 좁다**: 열린 초원(x1400–3300)과 세로 모바일 가시 대역
  (x1421–2419) 의 **교집합** = **x1421–2419, y1500–1850**. 프레임 가로의 26%뿐.
  왼쪽으로 더 가면 전경 풀 컷아웃이 필요하고, 오른쪽으로 더 가면 폰에서 화면 밖.
  즉 "들판을 가로지른다"가 아니라 "제자리 주변을 서성인다"가 현실적인 상한이다.
- **새 포즈**: 걷기·낮잠은 롤로스코핑으로 안 나온다(한 포즈뿐이라 옆으로 밀면
  다리는 가만있고 몸만 미끄러진다). 부위별 리깅(한 프레임을 머리/혀/몸/꼬리로
  잘라 애니메이트) 또는 신규 생성 — **별도의 아트 작업**이다.

---

## 6. 함정

- **크롭 상수는 `matte.py`와 `index.html` 양쪽에 있다.** 어긋나면 에러 없이
  몇 px 밀려 그려진다. 고칠 땐 반드시 양쪽 대조.
- **316프레임 유지.** 바꾸면 핑퐁 루프가 튄다.
- **transform은 한 요소에 하나씩.** 셰이크·폴짝·움찔·방향을 같은 요소에 쓰면
  에러 없이 서로 덮어쓴다. §3.6의 겹 구조를 지킬 것.
- **동작을 키우고 싶어도 §4.1 상한을 넘지 말 것.** 그림자가 배경에 남아 있다.
- `img/sky-<변형>.jpg`는 배경 영상의 첫 프레임이다. **패치 방식에서는 건드리지
  않는다** (구워 넣기 옵션 §7을 택할 때만 갱신).
- `_headers`가 `/img/*`에 `max-age=3600`. 새 파일이라 문제는 없지만, 자산을
  덮어쓰면 최대 1시간 옛것이 보인다. 즉시 확인은 하드 리로드.
- **numpy 없음.** PIL 밴드 연산으로 짜라. 4K를 파이썬 이중 루프로 돌리면
  프레임당 수십 초다 (`punch.py`가 그렇게 짜여 있지만 그건 1프레임짜리라 넘어간 것).
- zsh에서 `"$VAR[n]"`은 첨자로 해석돼 변수가 사라진다. `"${VAR}[n]"`.
- 색보정 필터(`eq`,`curves`,`selectivecolor`)는 RGBA를 안전하게 못 다룬다.
  이 계획에선 그레이딩을 안 하므로 해당 없지만, §7로 갈 땐
  `alphaextract` → 보정 → `alphamerge`.
- `og.jpg`는 그대로 둔다 (내루미가 들어간 공유 이미지).
- 검색엔진 비노출 유지: HTML은 `meta robots`, 자산은 `_headers`의 `X-Robots-Tag`.
- **커밋 = 배포.** 검증 전에 커밋하지 말 것.

---

## 7. 부록 A — (옵션) 패치를 영상에 구워 넣기

Phase 0이 확정된 뒤 런타임 레이어를 줄이고 싶으면. **git +108MB, 재인코딩 약
10분, `_headers` 캐시 1시간 지연**이 대가다.

`camp-overlay/build.sh` 5단계와 동일한 구조. 오버레이가 `plate-<변형>.png`
하나뿐이라 더 단순하다.

```sh
ffmpeg -i img/meadow-$V.mp4 -i img/plate-$V.png \
  -filter_complex "[0:v][1:v]overlay=0:0:format=auto,format=yuv420p[v]" \
  -map "[v]" -an -c:v libx264 -preset slow -crf 20 -movflags +faststart \
  -y img/meadow-$V.mp4.new
# HEVC는 -c:v libx265 -crf 22 -pix_fmt yuv420p10le -tag:v hvc1 (+bt709 3종)
# 정지 프레임도 반드시 방금 구운 영상에서 다시 뽑을 것
ffmpeg -i img/meadow-$V.mp4 -frames:v 1 -q:v 3 -y img/sky-$V.jpg
```

인코딩 후 `nb_frames == 316` 확인. 원본은 x264 crf21 / x265 crf23이고 재인코딩
세대 손실 상쇄로 한 단씩 올린 값이다.

## 8. 부록 B — 측정 재현 명령

```sh
# 영역별 프레임간 움직임 (YAVG = 차분 프레임의 평균 밝기)
ffmpeg -v error -i img/meadow-dusk.mp4 \
  -vf "crop=308:343:1866:1377,tblend=all_mode=difference,signalstats,\
metadata=print:key=lavfi.signalstats.YAVG:file=-" -f null -

# 변형 간 픽셀 정렬 확인: 엣지맵을 offset 0/1/2로 밀어 차이 비교
#   → offset(0,0)에서 최소면 정렬돼 있는 것
```

`plate.py`/`matte.py`가 쓸 키잉 기준값은 `tools/camp-overlay/punch.py`에 있다:
분홍기 `R − max(G,B)`가 잔디 18~24 / 내루미 40~67, 임계 32.
**하늘·언덕도 분홍이므로 y 1370~1780 밖에서는 키가 무의미하다.**
