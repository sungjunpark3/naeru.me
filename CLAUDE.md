# naeru.me — 내루미 시작페이지

**한 줄 요약:** 빌드 도구 없는 단일 `index.html` + 4K 배경 영상 8종(+장식 구운
`-camp` 6종). 시간대·날씨에 따라 배경 영상을 골라 크로스페이드하고 서울 시계를
띄운다. **화면 위에 DOM 장식을 얹지 말 것 — 장식은 영상 프레임에 굽는다.**

배포는 Netlify가 `origin/main`을 보고 자동으로 한다. 커밋 = 배포.

---

## 1. 구조

```
index.html   전부 여기 있음 (인라인 CSS/JS, 빌드 없음, 외부 의존은 웹폰트뿐)
_headers     Netlify 캐시·robots 정책
img/         배경 영상·정지 프레임·파비콘
tools/       자산 생성 스크립트 (사이트 구성요소 아님, 재현용)
```

### 배경 자산 명명 규칙 (JS가 문자열로 조립하므로 정확히 지킬 것)

| 파일 | 용도 |
|---|---|
| `img/meadow-<변형>.mp4` | H.264 배경 영상. **내루미가 구워진 원본 그대로다** — 2026-09 분리에서도 손대지 않았다 |
| `img/meadow-<변형>.hevc.mp4` | HEVC 10bit 병행본 (지원 브라우저 우선) |
| `img/sky-<변형>.jpg` | 해당 영상의 **첫 프레임**. poster + `--still` + 하늘 프리즈 3역 |
| `img/plate-<변형>.png` | **영상 속 내루미를 런타임에 지우는** 정지 패치. 대부분 투명, 내루미 자리만 불투명(§2) |
| `img/naeru-<변형>.webm` / `.mp4` | 내루미 알파 영상(VP9 / HEVC). DOM `#stage` 안에 얹힘 |
| `img/naeru-<변형>.png` | 내루미 정지본. poster + reduced-motion 대체 |
| `img/alpha-probe.webm` | 16×16 완전 투명 1프레임 — VP9 알파 지원 판정용 |

`<변형>` = `{dawn,day,dusk,night}` × `{"", -rain}` — 8종. 자산 생성 파이프라인은
`tools/naeru-split/`(§2), 배경 영상 자체의 생성 파이프라인은 git 히스토리에만
남아 있다(§4의 `tools/camp-overlay/`는 오버레이 전용이라 별개).

### 렌더링 레이어 (아래 → 위)

1. `.bg-still` — 정지 프레임 배경. 루프 순간 프레임 드랍 대비
2. `video.bg` A/B 2장 — 같은 푸티지의 색보정 변형이라 `currentTime`만 맞추면
   순수 색상 디졸브가 된다. 2.5s 크로스페이드
3. `.sky-freeze` A/B — 첫 프레임을 하늘 영역에만 덮어 구름 움직임 정지.
   마스크는 세로(위 42%~58% 페이드) ∩ 가로(16~28%, 68~80% 페이드)
4. `.plate` A/B — 배경 cover 그대로 깔리는 정지 패치. 좌표 계산 없음(§2)
5. `#stage` → `#naeru-move` → `#naeru-act` → `#naeru-face` → 내루미 — DOM
   내루미 본체. `.rain`·`.vignette` **아래**(구워져 있던 시절과 톤이 같도록)
6. `.rain` 2겹 — 영상에 굽지 않고 DOM에서 렌더(하늘 프리즈 위에서도 비가 옴)
7. `.vignette` → `#frame`(공) → `.clock-wrap`

### 시간대·날씨

- Open-Meteo 서울 실황(키 불필요) 5분 주기 + `visibilitychange` 재조회
- 밴드는 **실제 일출·일몰** 기준: dawn 일출−40~+50분 / dusk 일몰−50~+40분 /
  사이 day / 나머지 night. 실황 없으면 고정 밴드(05–08/08–17/17–20/20–05)
- `localStorage["naeru-weather"]` 3시간 캐시 → 재방문 0ms 부팅
- URL 강제: `?v=dawn|day|dusk|night` `?h=22` `?w=rain|clear` `?camp=0`

---

## 2. 배경은 내루미 없는 플레이트, 내루미는 DOM

2026-09에 내루미를 배경에서 떼어냈다(작업 지시서 `PLAN-naeru-split.md`,
자산 파이프라인 `tools/naeru-split/`). **배경 영상 16개는 건드리지 않았다** —
재인코딩하면 git이 108MB 늘고 되돌리기 어려워서, 영상 속 내루미를 런타임에
`plate-<변형>.png`(정지 패치)로 덮어 지우고 그 위에 알파 채널이 있는
`naeru-<변형>.webm`/`.mp4`를 `#stage` 안에 DOM으로 얹는 방식을 택했다.
그래서 배경 자산을 다시 구울 일이 생기면 patch도 같이 다시 만들어야 한다.

**그래도 좌표 계산을 개체마다 하는 방식은 여전히 금지다.** 배경은
`object-fit: cover`로 그리므로 화면비에 따라 잘리는 방향·배율이 달라진다.
DOM 요소를 배경 속 사물에 개체별로(예: cover 변환을 직접 계산해서) 맞추면
화면비가 바뀔 때마다 어긋난다 — 2026-07-21 생일 장식(꼬깔모자를 cover
변환으로 계산해 머리에 얹음)이 이 이유로 커밋 dc80530 → 5f5a963로 되돌려졌다.
지금도 똑같다: **좌표는 반드시 `#frame`/`#stage` 상자 안에서 프레임 비율(%)로만
쓴다.**

### `#frame`과 `#stage` — cover 계산을 한 곳에만

둘 다 `layoutFrameBox()`(index.html 상단의 전역 함수) 하나로 크기·위치를
잡는다 — 배경 영상이 cover로 실제 차지하는 16:9 사각형을 뷰포트에 맞게
계산해서 그 요소에 그대로 적용한다. 그 안에 들어간 자식은 프레임 비율로
좌표를 쓰므로 화면비가 뭐든 배경과 같이 움직인다(6가지 화면비에서 검증됨).

- **`#frame`** — 랜덤 타이밍이 필요한 것(공 콩)이 들어간다. 316프레임 핑퐁
  루프라 13초마다 반복되고 후반부가 역재생이라 영상에 못 굽는 것들.
- **`#stage`** — 내루미 본체가 들어간다. `.rain`/`.vignette`와 같은 레벨이라
  DOM 순서로 쌓인다(§1) — **z-index를 주지 말 것**.

### 내루미의 transform 겹 구조

`transform`은 요소 하나에 하나만 붙는다. 두 동작을 같은 요소에 쓰면 서로
덮어쓰므로, 역할별로 요소를 나눠 각자 자기 transform만 소유한다:

| 요소 | 담당 |
|---|---|
| `#stage` | 배경 셰이크(`--bg-shake`, 공 맞을 때) |
| `#naeru-move` | 폴짝·이동 (`translateY`) |
| `#naeru-act` | 움찔·두리번 (`rotate`·`translateX`·`scale`) |
| `#naeru-face` | 방향 (`scaleX(-1)` 좌우 뒤집기) |

회전·스쿼시의 기준점(`transform-origin`)은 **발끝**이다: `47.9167% 79.0323%`
(크롭 상단이 y1328, 발끝이 y1720이므로 세로 (1720-1328)/496 = 79.03%; 몸
중심 x2020 → (2020-1744)/576 = 47.92%). 틀리면 기울일 때 발이 지면에서 뜬다.

**새 장식(모자·소품)은 `#naeru-face`의 자식으로 붙이면 폴짝·움찔·뒤집기를
전부 따라간다.** 작년 생일 꼬깔모자를 되돌린 이유(cover 변환을 개체마다
계산해야 했던 것)가 여기서는 사라진다 — `#naeru-face` 안에서는 그냥 %
좌표만 쓰면 된다.

### 동작 진폭의 상한은 "구워진 그림자"가 정한다

접지 그림자는 plate가 지우지 않고(발끝 아래는 페이드아웃, §4) 배경 영상에
그대로 남아 있다. 그래서 내루미가 움직이면 그림자가 따라오지 않는다 —
이게 모든 동작의 진폭 상한이다.

| 움직임 | 그림자와의 관계 | 상한 |
|---|---|---|
| 수직(폴짝) | 수평 정렬 유지 → 가장 안전 | 자기 키의 15%(≈70px@4K) |
| 수평(움찔·두리번) | 그림자가 그만큼 뒤처짐 | ±20px@4K(몸 폭의 6%) |
| 회전 | origin이 발끝이라 접지점은 안 움직임 | 2.5deg |

넘으면 "발이 미끄러진다"/"그림자가 안 따라온다"가 눈에 띄기 시작한다.

### 크롭 상자 — matte.py ↔ index.html의 계약

내루미 알파 영상의 크롭 사각형(`tools/naeru-split/coords.py`의
`CROP_ORIGIN`/`CROP_SIZE`, 현재 1744,1328 / 576×496 @ 3840×2160)이
`#naeru-move`의 `left/top/width/height` %로 그대로 옮겨 적혀 있다. **어긋나면
에러 없이 내루미가 몇 px 밀린 채로 그려진다.** 크롭을 고치면 반드시
`coords.py`와 `index.html`의 `#naeru-move`, `#naeru-act`/`#naeru-face`의
`transform-origin`, `#naeru-head`(공 IIFE의 조준점) 셋을 같이 대조할 것.

### 화면비별 가시 영역 (장식 배치 시 필수 고려)

`cover`라 좁은 화면에선 가로가, 넓은 화면에선 세로가 잘린다.

| 화면 | 보이는 프레임 영역 |
|---|---|
| 16:9 | 전체 |
| 16:10 노트북 | 가로 5~95% |
| 4:3 | 가로 12.5~87.5% |
| 21:9 울트라와이드 | 세로 13~87% |
| 세로 모바일(9:19.5) | **가로 37~63%만** |

→ 핵심 요소(현수막 글자, 내루미, 풀장)는 가로 37~63% 안에 넣는다.
→ 세로는 13~87% 안. 상단은 시계(8vh~26vh)와도 겹치지 않게.

### 프레임 좌표 기준점 (3840×2160)

| 대상 | 좌표 |
|---|---|
| 내루미(정적 참고) | x 1866–2174, y 1377–1720 (발끝 1720) |
| 내루미 머리 꼭대기 | (1995, 1399) = 프레임 비율 (0.520, 0.648) |
| 내루미 크롭 박스(위 §) | x 1744–2320, y 1328–1824 (576×496) |
| 지평선 | y ≈ 1270 |
| 왼쪽 나무 줄기 | x 0–384 (정지 영역, 밧줄 고정점으로 적합) |
| 오른쪽 나무 줄기 | x 3530–3840 |
| 열린 초원 | x 1400–3300, y 1500–1850 |
| 움직임 많은 곳 | 나무 수관(x 20–40%, y 0–40%), 앞쪽 풀(x 10–30%, y 70%) |

---

## 3. 지난 작업 — 여름수련회(신덕수양관) 장식 · **현재 미적용**

2026-08-01~02에 `-camp` 변형 6종을 만들어 배포했다가 08-03에 되돌렸다.
현수막·삼각깃발·랜턴·텐트 2동·파란 사각 풀장·튜브·비치볼, 그리고 **풀장 안에
들어가 "문지은T" 캡모자를 쓴 내루미**. 밤 변형은 만들지 않았다.

지금 `index.html`과 `img/`는 장식 이전 상태다. 다시 켜려면:

1. `./tools/camp-overlay/build.sh` — `-camp` 자산 18개 재생성 (약 7분)
2. `index.html`의 `srcOf`/`freezeOf`/`poster`가 변형 이름에 `-camp`를 붙이게
   한다. 되돌린 커밋(`3e95fc8`)에 있던 형태:

```js
var CAMP = params.get("camp") !== "0";
function campOf(v) {
  return (CAMP && v.indexOf("night") !== 0) ? v + "-camp" : v;
}
```

`data-variant`는 건드리지 않아야 CSS 선택자(`body[data-variant="day"]` 등)가
안 깨진다. 재생 실패 시 HEVC → H.264 → 장식 없는 원본으로 후퇴시켜 뒀었다.
아트·좌표·마스크는 `tools/camp-overlay/gen_svg.py`에 그대로 남아 있다.

---

## 4. 자산 생성 파이프라인 (`tools/camp-overlay/`)

`./tools/camp-overlay/build.sh` 한 방이면 6변형 18개 파일이 전부 다시 만들어
진다(약 9분). 중간 산출물은 `build/`(gitignore).

```
gen_svg.py back|front  → 3840×2160 SVG (HTML로 감싼 것)
punch.py               → back 레이어 알파에 내루미 실루엣 구멍
build.sh               → 렌더 → 구멍 → 그레이딩 → 합성·인코딩 → 첫 프레임 jpg
```

핵심 2가지:

**(a) 색 맞추기.** 무보정(=dusk) 톤으로 한 장만 그리고, 배경 영상을 만들 때 쓴
밴드별 그레이딩 체인을 오버레이에도 똑같이 먹인다. 그래야 색이 붙는다.

**(b) 구워진 캐릭터 앞뒤로 물건 놓기.** 내루미는 영상에 구워져 있어 그 뒤에
뭔가를 두려면 덮어 그릴 수가 없다(덮으면 캐릭터가 지워진다). 그래서 레이어를
둘로 나눈다.

- `back` — 내루미 뒤에 깔릴 것 전부(현수막·텐트·풀장). 렌더 후 **알파에서
  내루미 실루엣을 도려낸다.** 구멍 자리엔 원본 영상 픽셀이 그대로 보이므로
  캐릭터의 미세한 숨쉬기 모션(1~3px)까지 살아남는다
- `front` — 몸 위에 올라가야 하는 것(모자·수면선 물결)

실루엣은 **분홍기 키잉**으로 뽑는다: `R - max(G,B)`가 잔디 위에서 18~24,
내루미가 40~67이라 임계 32면 깨끗하게 갈린다. 단 **하늘·언덕도 분홍이라
잔디를 등진 y 1400~1700 구간에서만 유효**하다(그 위쪽은 어차피 아무것도 안
그리므로 상관없음). 닫기→열기로 정리하고 3px 팽창 + 4px 블러 —
캐릭터가 몇 px 흔들려도 몸 가장자리가 테두리에 먹히지 않는다.

수면선(`WATER_Y`) 아래는 구멍을 뚫지 않는다 → 다리가 물에 잠긴다. 이 값은
`gen_svg.py`와 `punch.py` **양쪽에 있으니 같이 고칠 것.**

### 밴드별 그레이딩 체인 (배경 영상 제작 시 사용한 것과 동일)

- **dusk**: 원본 무보정 (identity)
- **dawn**: `colortemperature=7800:mix=.75` → `selectivecolor reds='0.15 0.10
  -0.25 0' yellows='0.10 0.05 -0.30 0' whites='0 0 -0.15 0'` → `colorbalance
  rs=-.03:bs=.09:rm=.03:bm=.05:rh=.05:bh=.04` → `eq brightness=.05:gamma=1.06:
  saturation=.93` → `vibrance .10:gbal=2:rbal=.2:bbal=.2` → `curves '0/0.055
  0.5/0.54 1/1'`
- **day**: `colortemperature=12000:mix=.92` → `selectivecolor reds='0.45 0 -0.35
  0' yellows='0.18 0 -0.22 0' whites='0 -0.06 -0.28 0.06' magentas='0.25 -0.05 0
  0'` → `eq brightness=.08:gamma=1.13:saturation=1.06:contrast=1.02` →
  `vibrance .15:gbal=1.5` → `curves '0/0 0.5/0.56 1/1'`
- **night**: `selectivecolor reds='0.55 0 -0.25 0.30' yellows='0.55 0 -0.30
  0.35'` → `eq saturation=.42:gamma=.74:brightness=-.11:contrast=1.05` →
  `colorbalance bs=.17:bm=.14:bh=.09:rs=-.06:rm=-.08:rh=-.07` → `curves '0/0
  0.6/0.40 1/0.72'`
- **rain 계열**(밝기만 밴드별 차등, 저채도·저대비·쿨캐스트)
  - dawn-rain `eq sat=.28:br=-.02:gamma=.95, cb bs=.10:bm=.08:bh=.05, curves
    '0/0.05 0.5/0.47 1/0.74'`
  - day-rain `eq sat=.30:br=.02:gamma=1.02, cb bs=.06:bm=.05:bh=.04, curves
    '0/0.06 0.5/0.52 1/0.82'`
  - dusk-rain `eq sat=.28:br=-.04:gamma=.90, cb bs=.09:bm=.07:bh=.05, curves
    '0/0.04 0.5/0.44 1/0.68'`
  - night-rain: night의 웜톤 중화(selectivecolor)를 **반드시 먼저** + `eq
    sat=.22:gamma=.70:br=-.13:contrast=1.03, cb bs=.15:bm=.12:bh=.08, curves
    '0/0 0.6/0.34 1/0.52'` — 빠뜨리면 지평선에 노을 잔광이 남는다

### 인코딩 설정

원본은 x264 crf21 / x265 10bit crf23. **재인코딩 세대 손실을 상쇄하려고 한 단
올린다.**

```sh
# H.264
-c:v libx264 -preset slow -crf 20 -pix_fmt yuv420p -movflags +faststart
# HEVC (Safari/Chromium 하드웨어 디코드용, hvc1 태그 필수)
-c:v libx265 -preset medium -crf 22 -pix_fmt yuv420p10le -tag:v hvc1 \
  -color_primaries bt709 -color_trc bt709 -colorspace bt709
```

4K 316프레임 기준 M-시리즈에서 x264 ~26초, x265 ~45초/편.

### 영상 규격 (건드리지 말 것)

- 3840×2160, 24fps, **316프레임 완전 핑퐁 루프** (`[0..158] + [157..1]` —
  마지막 중복 프레임을 뺐기 때문에 랩 스터터가 없다). 프레임 수를 바꾸면 루프가
  튄다
- 업스케일 전 원본 2560×1440 317프레임은 git 히스토리 `img/meadow.mp4`에 있다
- 화질 상한 확인됨(2026-07-11): 이 회화풍 푸티지에선 crf21이 이미 투명.
  재인코딩으로 체감 화질이 오르지 않는다

---

## 5. 함정

- **분홍기 키를 소프트 램프로 쓰지 말 것.** dusk 실측(2026-09-01): 배경(잔디·
  언덕)의 `R−max(G,B)`가 p50=16 **p99=25~27**, 내루미가 p50=41 **하위 5%=15~18**.
  두 분포가 꼬리에서 겹치므로 램프(예: 22→40)로 알파를 만들면 배경이 10~50%로
  통째로 딸려 들어와 스프라이트가 반투명 후광을 달고 다닌다. **임계 30으로
  이진**으로 자르고, 겹치는 몸 하위 5%(어두운 외곽선·배 크림줄무늬)는
  flood-fill 구멍메움으로 되찾는 게 맞다. 경계 AA는 이진 마스크의 블러로.
- **`plate`와 스프라이트는 서로의 오차를 상쇄한다 — 복원 PSNR로는 분리 실패를
  못 잡는다.** `C = P + (O−P)/a`라 `P≈O`이면 알파가 뭐든 원본이 복원된다.
  plate에 캐릭터가 남고 알파에 배경이 섞인 상태로도 41dB가 나왔다(2026-09-01).
  **plate 단독·스프라이트 단독으로 각각 확인할 것** — `verify.py`의 GATES가
  이걸 본다. 눈으로 볼 땐 자홍색 배경 위에 얹어보면 바로 드러난다.
- **키잉창 상단은 y1394다.** 1370~1392에 먼 언덕의 분홍기가 임계를 넘는 너덜한
  가로 띠가 잡히고, 좁은 목으로 머리와 이어져 연결성분 필터로도 안 떨어진다.
  머리 돔 꼭대기는 316프레임 전부 y1398~1402.
- **덧칠 자리는 LaMa 인페인팅으로 채운다 — 전용 venv가 필요하다.**
  `tools/naeru-split/.venv`(torch ~900MB, gitignore). 없으면 `plate.py`가 설치
  명령과 함께 멈춘다. 옆에서 떠온 조각을 색 맞춰 붙이던 이전 방식은 구조는
  맞아도 뿌옇고 도너의 나무·풀숲이 딸려와 "물감 지운 자국"으로 보였다
  (2026-09-04 제보). 모델을 붙이자 PSNR도 42.5→44.5dB로 올랐다.
  **덮는 범위를 좁혀 해결하려 들지 말 것** — 24px 팽창을 줄이면 보이는 합성
  영역은 절반이 되지만 붓질로 번진 원본 캐릭터 윤곽이 되살아난다(실측).
- **시간축 채우기는 몸통 핵심부를 못 메운다.** 내루미가 316프레임 내내 같은
  자리에 서 있어서, "가장 안 가려진 프레임"을 골라도 늘 가려지는 화소는 표본이
  없다. `plate.py`의 `inpaint_core()`가 **같은 행**의 깨끗한 초원을 통째로
  가져다 쓰고 색 차이만 확산시켜 보정한다 — 같은 y라서 언덕·경계선·풀 높이가
  원래 맞는다. 조화 보간(주변 색만 확산)은 경계선을 뭉개 내루미 모양 얼룩이
  남고, 행별 가로 보간은 가로 줄무늬가 된다. 둘 다 해봤다.
- **누끼를 눈으로 판정하지 말고 띠별 밝기차로 재라.** 구워진 원본(`git show
  67ded1e:index.html`을 `_baseline.html`로 띄운다)과 현재 페이지를 같은 크기로
  렌더해서, 실루엣 바깥 0~2 / 2~4 / 4~7 / 7~12px 띠의 **평균 밝기차**를 본다.
  0~2px만 양수로 뜨면 외곽선이 잘려 옅은 후광이 생긴 것이다(실측 +2.9 →
  링 복원 후 +0.5). 눈으로만 보다가 세 번 놓쳤다.
- **좁은 링에서는 `|O-P|`를 믿어도 된다.** plate의 그 자리는 시간축 채우기가
  원본 화소를 그대로 쓴 진짜 배경이다(링 0~4px와 14~22px 평균밝기 126.7/126.6로
  평탄한 걸로 확인). 그래서 실루엣 밖 6px~안 2px 링에서만 색거리로 알파를
  **보태면**(빼지 말 것) 잘린 외곽선이 돌아오고 배경은 안 딸려온다. 실루엣
  전체에 같은 걸 쓰면 어두운 외곽선이 어두운 언덕과 안 갈려서 머리가 반투명해진다.
- **접지 밴드(y1680 아래)는 도너로 채우지 말 것.** 도너는 볕 드는 잔디라
  접지 그림자가 지워지고 캐릭터가 떠 보인다. 주변에서 확산시켜 어둠을 물려받게
  하되, **확산 소스는 구멍 전체 바깥**이어야 한다 — 밴드만 구멍으로 치면 위쪽
  몸통(아직 캐릭터 색)에서 끌어와 발밑이 분홍으로 번진다.
- **매트 품질은 정지 화면에서 원리적으로 안 보인다.** 스프라이트 색을
  `C = P + (O-P)/a`로 역산하므로, 알파가 뭐든 plate 위에 다시 얹으면 원본이
  복원된다. **움직여봐야 드러난다** — 검사는 반드시 스프라이트를 몇십 px 옮겨
  합성해서 볼 것. 2026-09-01에 톱니 경계·경계 들끓음·빠진 발이 전부 이 때문에
  늦게 발견됐다.
- **이진 임계 실루엣은 윤곽을 평활해야 한다.** 분홍기가 노이즈 위에서 갈리며
  ±2px 톱니를 만들고 닫기(9px)가 그걸 4px 계단으로 뭉친다. 블러(3.5) → 재이진화
  로 펴면 모양은 유지되고 톱니만 사라진다. 프레임마다 독립으로 키를 뜨면 경계가
  들끓으므로(boiling) **시간축 3프레임 중앙값**도 같이 건다(실측 16.9 → 12.1,
  모션은 3.8 유지). 부수 효과로 프레임간 변화가 줄어 자산이 23MB → 11MB가 됐다.
  **중앙값만으로는 부족하다 — 1-2-1 저역통과를 한 번 더 건다.** 중앙값은 한
  프레임짜리 튐만 없애고, 경계가 매 프레임 ±1px씩 떠는 자글거림은 남는다
  (2026-09-04 제보 "약간씩 누끼가 뜨는 프레임이 보여"). 재는 법: 경계 밴드에서
  **시간축 라플라시안** `|A_i - (A_{i-1}+A_{i+1})/2|` — 부드러운 모션은 이웃
  평균으로 예측되므로 0에 가깝고 자글거림만 남는다. 프레임간 차분을 그냥 재면
  실제 모션이 섞여 못 갈린다(처음에 이걸로 헛짚었다). 저역통과 후 p99 52 → 27.
  핑퐁이라 양 끝은 순환으로 이어 재고, **인덱스 0·315는 이음매라 값이 크게
  나오는 게 정상**(0=src0, 1과 315가 둘 다 src1).
- **흰 발은 분홍기로 안 잡힌다.** 발 구간 알파의 72%가 0이었다. plate가 발을
  지우면(접지 페이드 시작선 y1745) 스프라이트가 다시 그리질 못해 발 없는
  내루미가 된다. 그 구간에서만 배경과의 색거리로 알파를 보태되(흰 발 vs 초록
  잔디라 p90=62로 잘 듣는다) **plate가 지운 양을 상한으로** 둬서 그 아래
  그림자는 안 건드린다.
- **HEVC 알파는 프리멀티플라이로 넣어야 한다 — 사파리 전용 함정.** 사파리는
  HEVC 알파 영상을 `dst = src + (1-a)*bg`로 합성한다. 스트레이트 RGB를 그대로
  넣으면 경계 화소에서 스프라이트 색과 배경이 **더해져** 실루엣을 두르는 밝은
  크림색 테두리가 생긴다(2026-09-01 사파리 18.6에서 나란히 렌더해 확인).
  `build.sh`의 HEVC 인코딩에만 `-vf premultiply=inplace=1`을 건다 —
  **webm(VP9)은 스트레이트가 맞으므로 걸면 안 된다.**
  진단이 오래 걸린 이유: 캔버스 `drawImage`로 읽으면 사파리도 알파를 정확히
  내놓는다(투명 구석 0,0,0,0 / 초록 위 합성도 순수 초록). **합성기 경로에서만
  틀리므로 화면 캡처로만 보인다.** 다음에 비슷한 증상이면 캔버스 측정을
  믿지 말고 `screencapture`로 실제 화면을 찍을 것.
- 참고: 사파리는 **VP9 webm의 알파를 통째로 무시**하고 불투명하게 그린다
  (측정: 구석 알파 255, 반투명 화소 0개). alpha-probe가 이걸 걸러 mp4로 보낸다.
- **자글거림을 인코딩 화질로 해결하려 들지 말 것.** 알파도 손실 압축이라 의심이
  가지만 실측(2026-09-04)으로 아니었다: 소스 p99 26.5 → VP9 crf34 27.0 /
  HEVC q40 26.5. crf를 34→24→16, q를 40→65→85로 올려도 지표가 그대로이고
  파일만 0.4MB → 2.3MB, 0.5MB → 3.0MB가 된다. 실루엣 바깥 후광도 crf34에서
  프레임평균 0.09(최대 알파 9)로 이미 무시할 수준. **소스 매트를 고쳐야 한다.**
- **알파 영상의 투명 영역 RGB도 청소해야 한다.** 언프리멀티플라이는 알파가 0에
  가까우면 최대 6.7배로 외삽하므로 투명 자리에 극단색이 남는다. VP9 알파는 그
  색을 안 쓰지만 **HEVC 알파는 4:2:0 크로마라 경계 화소로 번져서** 사파리에서만
  내루미 둘레 색이 튄다. `matte.py`의 `bleed_transparent()`가 알파 가중 블러로
  몸 색을 바깥에 채운다 — 알파 영상을 만들 땐 어느 파이프라인이든 필요한 단계다.
- **plate와 내루미는 같은 프레임에 켜고 꺼야 한다.** 따로 켜지면 "구워진 내루미가
  지워진 빈 초원" 또는 "구워진 것과 DOM 것이 겹친 이중노출"이 눈에 보인다 —
  첫 접속이 오류처럼 보이던 원인. 페이드 중 겹치는 건 무해하다(지우는 양과
  그리는 양이 같은 불투명도로 상쇄된다).
- **영상을 새로 추가하면 `visibilitychange` 핸들러에도 넣을 것.** 내루미 영상을
  빠뜨려서 탭을 한 번 숨기면 그대로 멈췄고, poster가 깔려 있어 "정지한 내루미"로
  그럴듯하게 보였다(숨쉬는 모션이 사라졌다는 제보가 이것). `watchPlayback()`이
  안 흐르는 걸 감지해 재시도→코덱 교체→정지본으로 내린다.
- **zsh에서 `"$VAR[n]"`은 첨자로 해석돼 변수가 통째로 사라진다.** ffmpeg 필터
  라벨을 조합할 땐 `"${VAR}[n]"`
- **Edge 헤드리스 스크린샷은 파일을 쓰고도 프로세스가 안 죽는다.** 백그라운드로
  띄우고 파일 생성을 폴링한 뒤 **해당 PID만** kill. 사용자가 Edge를 상시 쓰므로
  광역 `pkill` 금지
- 색보정 필터(`eq`, `curves`, `selectivecolor` …)는 RGBA를 안전하게 못 다룬다.
  `alphaextract` → RGB 보정 → `alphamerge`로 알파를 따로 보존할 것
- `sky-<변형>.jpg`는 반드시 **같은 영상의 첫 프레임**이어야 한다. 영상을 다시
  구우면 이 jpg도 같이 갱신할 것 (안 하면 하늘 프리즈만 장식 없는 옛 그림)
- **`plate-*.png`·`naeru-*.*`를 다시 구우면 `index.html`의 `ASSET_V`를 올릴 것.**
  `_headers`의 `stale-while-revalidate=604800` 때문에 브라우저는 max-age가 지나도
  이번 방문엔 옛 파일을 쓴다. 하루에 여러 번 배포하면 plate와 스프라이트가 서로
  다른 세대로 섞이고, 그러면 실루엣을 두르는 테두리가 생긴다(2026-09-01에 겪음 —
  자산은 멀쩡한데 화면에만 후광이 보였다). 판번호를 붙이면 한 벌로 받는다.
- `_headers`가 `/img/*`에 `max-age=3600`을 주므로 자산을 교체해도 최대 1시간은
  옛 파일이 보일 수 있다. 즉시 확인하려면 하드 리로드
- 검색엔진 비노출: HTML은 `meta robots`, 나머지 자산은 `_headers`의
  `X-Robots-Tag`가 담당. 둘 다 유지할 것
- `prefers-reduced-motion`이면 영상을 아예 로드하지 않고 정지 프레임만 쓴다.
  장식은 정지 프레임에도 들어가 있어야 이 경로에서 보인다

---

## 6. 코드 스타일

- 주석은 한국어. **왜 그렇게 했는지**를 적는다 (무엇을 하는지는 코드가 말함)
- 섹션 구분은 `/* ── 이름 ─────── */`
- 빌드 스텝·번들러·프레임워크 도입 금지. `index.html` 한 장을 유지한다
- 기능은 독립 IIFE로. 한 곳이 터져도 시계·배경은 살아야 한다
