# naeru.me — 현재 작업 기준

단일 `index.html`로 실행되는 시작페이지다. **계절 배경 정지 이미지 32장 위에
투명 내루미 영상을 올리고**, 서울 시계·날씨와 작은 동작을 표시한다.
화면 설정과 25·50분 집중 타이머를 제공한다. 앱 빌드 단계나 프레임워크는 없다.

이 문서는 현재 구조의 기준이다. 과거 시행착오와 제작 수치는
[작업 기록](docs/HISTORY-2026-09-08.md)에 보존했다. 기록 앞부분의 배경 영상·
플레이트 런타임 설명은 현재 구조와 다르다. 실행법은 [README](README.md),
이번 변경은 [변경 내역](docs/CHANGES-2026-09-11.md)을 참고한다.

## 지킬 원칙

- `index.html` 한 장에 인라인 CSS·JS를 유지한다. 앱 빌드·번들러·프레임워크를
  도입하지 않는다. `tools/browser-check`의 Node 의존성은 개발 검사용이다.
- 동작은 독립 IIFE로 두고, 공유 상태와 이벤트는 필요한 것만 사용한다.
- 개체마다 cover 좌표를 계산하지 않는다. `layoutFrameBox()`가 맞춘
  `#stage`·`#frame` 내부에서 원본 프레임 비율로 배치한다.
- `noindex, nofollow`와 `_headers`의 검색 제외 정책을 유지한다.
- 수련회 장식·낙엽 낙하는 미적용 상태를 유지한다. 혀 동작은 URL로만 켠다.
- Netlify는 `origin/main`에 push하면 자동 배포된다. 로컬 커밋만으로는
  배포되지 않는다. 검증에 필요한 스크린샷은 임시 파일로만 확인한다.
- 별도 보고자료나 `_report`·`_reports` 폴더를 만들지 않는다.
  결과는 채팅으로 간결하게 전달한다.
- `_redirects`는 개발 문서·도구·제작 입력의 HTTP 접근을 강제 404로 막는다.
  원본은 재생성을 위해 보존한다. 이 규칙이 배포 파일 용량을 줄이지는 않는다.

## 파일과 자산

| 경로 | 역할 |
|---|---|
| `index.html` | 화면·설정·날씨·플레이어·동작·집중 타이머 |
| `_headers`, `_redirects`, `404.html` | Netlify 캐시·검색 제외·경로 차단 |
| `img/bg-<변형>-<계절>.jpg` | 런타임 배경 32장, 3840×2160 |
| `img/foreground-<변형>-autumn.webp` | 가을의 투명 꽃·앞풀 8장, 3840×2160 |
| `img/naeru-<변형>.webm`, `.mp4` | VP9·HEVC 알파 캐릭터 영상 16개 |
| `img/naeru-<변형>.png` | 영상 로딩·실패·정지 모드용 캐릭터 8장 |
| `img/naeru-<변형>-hd.webp` | 근접·정지 화면용 4608×3968 캐릭터 8장 |
| `img/naeru-<시간대>-close.webp` | 얇은 선으로 그린 근접 전용 원화 4장 |
| `img/naeru-<변형>-nt.png`, `tongue-<변형>.png` | URL로 켜는 혀 동작용 |
| `img/alpha-probe.webm` | VP9 알파 지원 검사 |
| `tools/naeru-split/` | 캐릭터 분리·인코딩·배경 합성 |
| `tools/naeru-hd/` | 뉴트럴 프레임 8배 복원·투명 경계 정리 |
| `tools/ground/` | 고정 그림자가 없는 잔디 원본·배경 합성 |
| `tools/foreground/` | 가을 앞풀 제거 원화·전경 원화·시간대 조명 변환 |
| `tools/season/repaint.py` | 계절 배경 제작, `--seasons winter` 등 부분 실행 |
| `tools/check-assets.py` | 100개 자산·좌표·경로·자산 판번호 검사 |
| `tools/browser-check/` | 실제 Chromium에서 상태 전환·입력·화면 검사 |

`<변형>`은 `{dawn,day,dusk,night}` × `{빈 접미사,-rain}`의 8종,
`<계절>`은 `spring,summer,autumn,winter`다. 여름도 `-summer`를 붙인다.
`meadow-*`, `sky-*`, `plate-*`, 계절 접미사가 없는 `bg-*`는 제작 입력이다.

## 화면·좌표·움직임

레이어는 배경 정지본/A·B → `#stage`(내루미) → `#foreground`(가을 앞풀) → 비·눈 → 비네트 →
반딧불이·`#frame`(공·나비) → 시계 → 화면 조작 UI 순이다.
설정은 native `dialog`의 top layer를 사용한다.

- 좌표 계약: `tools/naeru-split/coords.py`, `build.sh`와 CSS를 함께 대조한다.
  원본 3840×2160, 크롭 원점 (1744,1328), 크기 576×496.
  캐릭터 영상은 24fps·316프레임이다.
- `#naeru-move`는 이동, `#naeru-act`는 몸짓, `#naeru-face`는 좌우 방향을
  담당한다. 반전은 공중에서만 한다. 좁은 세로 화면은 중앙 약 37~63%만 보인다.
- 가을 맑은 새벽·낮·노을·밤의 다가오기는 `#naeru-approach`의 원근 이동·확대와
  `#naeru-stride`의 보폭을 별도로 합성한다. `#naeru-move`의 크롭 계약은 같다.
  배경의 고정 그림자는 제작 단계에서 제거한다. 그림자는 항상 DOM으로 표시한다.
  방문 조건과 화질 한계는 [현재 동작 기록](docs/APPROACH-2026-09-15.md)에 있다.
- 가을 배경 8장에서는 좌우의 가까운 꽃·풀을 지우고 나무·들판을 복원했다.
  새 투명 원화인 `#foreground`는 접근 전후 계속 표시하며, 배경과 같은 cover
  좌표와 흔들림을 공유한다. 내루미의 확대·보폭과 계절 필터는 받지 않는다.
  배경 A/B와 같은 세대로 전환하고 정지 모드에서도 표시한다. 로드를 4초까지
  기다리며 실패·지연 시 꽃 없는 들판으로 진행한다. 뒤늦은 응답을 현재 장면에
  끼워 넣지 않는다. `repaint.py --seasons autumn`이 배경과 전경을 함께 재현한다.
  기존 SVG 꽃 색상 마스크는 제거했다.
- `#naeruHd`는 정지 애니메이션용 RRDB와 SRVGG로 RGB를 8배 복원하고,
  근접용 알파 경계를 정리한 뉴트럴 그림이다.
  정지 화면과 근접 원화 실패 시 사용한다. HD는 비동기로 로드하며
  실패·지연 시 원래 자산을 사용한다. 시작한 방문 도중 그림을 교체하지 않는다.
  정지본도 HD 디코드 후 같은 위치에서 교체한다.
  [그림자·8배 복원 기록](docs/SHADOW-HD-2026-09-16.md) 참고.
  최대 접근에서 남은 흐림과 후속 개선은
  [근접 화질 기록](docs/CLOSEUP-QUALITY-2026-09-16.md)에 있다.
- `#naeruClose`는 머리·볼·팔·혀의 과한 선 굵기를 다시 그린 근접 원화다.
  접근 가능한 가을 맑은 장면에서만 네 시간대 파일을 준비한다. 접근 시작 시
  준비된 원화를 선택하고 진행률 4~24%에서 영상과 교차 전환한다.
  출발 후 늦게 도착한 원화로 바꾸지 않는다. 실패하면 기존 HD→원래 영상 순으로
  대체하며, 복귀 후 정지본은 항상 기존 HD를 쓴다. `closeup.py`로 원화를
  크롭 좌표에 정렬하고 같은 형태에 시간대 색을 입힌다.
- `#naeru-shadow`는 `#naeru-move`와 같은 크롭 좌표를 가진 형제다.
  `#shadow-cast`는 넓은 투영 그림자, `#shadow-contact`는 발밑 접촉 그림자다.
  `naeruShadow.lift()`는 지면 Y를 유지하고 농도·퍼짐만 바꾼다.
  배우의 `setMove()`는 높이(크롭 대비 %)를 받아 점프와 그림자를 함께 갱신한다.
  접근은 `naeruShadow.travel()`에 같은 원근 transform을 주되 보폭은 분리한다.
  반환·중단 시 두 값을 초기화한다. 정지 모드와 `act=0`에서도 그림자는 표시한다.
- `--grade`와 `--season`은 하나의 `filter`에서 합성한다.
  중립값은 `brightness(1)`이다. `none`을 이어 붙이면 전체 필터가 무효다.
- `createSceneMotion()`이 각 IIFE의 rAF·타이머를 추적한다.
  `naeru:scene`·`visibilitychange`에서 취소하고 transform·busy·hold를 정리한
  뒤, 장면 준비·탭 표시·동작 허용 조건이 맞으면 재시작한다.
- 공은 프레임 횟수 대신 경과 시간으로 계산하고 착지 상태·반동 횟수를 제한한다.
  중단되거나 끝나면 반드시 `window.naeru.busy`를 해제한다.
- 나비·혀·다가오기가 영상을 의도적으로 멈추면 `data-pause-owner`를 지정한다.
  플레이어 감시가 이를 재생 실패로 오인하지 않게 한다.
- 플레이어는 세대 번호로 오래된 비동기 응답을 무시한다. 배경·정지본 준비 후
  장면을 함께 바꾸고, 영상 실패 시 정지본을 유지한다. 코덱 판정은 움직임이
  필요할 때만 한다. 정지 모드에서 영상·프로브를 요청하지 않는다.
- 키보드 Enter·Space와 클릭은 `#naeru-touch` → `naeru:greet`로 연결한다.
  첫 방문 힌트는 `naeru-hint-seen`에 기록한다.

## 서울 시간·날씨·설정

- 시간·날짜·자동 계절은 `Asia/Seoul` 기준이다. 계절은 3~5월 봄,
  6~8월 여름, 9~11월 가을, 12~2월 겨울이다.
- Open-Meteo를 5분마다 조회한다. 탭 복귀 시 데이터가 1분 이상 지났으면
  재조회한다. 요청은 중복하지 않고 8초 후 취소한다. 시계는 응답을 기다리지 않는다.
- 날씨 캐시는 `localStorage["naeru-weather"]`의
  `{cd,tp,sr,ss,sr2,day,t,w}`다. `t`는 마지막 성공 시각, 일출·일몰은 분 단위다.
  부팅 시 3시간 이내 캐시를 사용한다. 실패 응답으로 캐시를 갱신하지 않는다.
  조회 실패나 마지막 성공 이후 15분 경과 시 `갱신 지연`을 표시한다.
- 일출−40~+50분은 새벽, 일몰−50~+40분은 노을이다. 당일 일출 정보가
  없으면 05~08/08~17/17~20/20~05시를 새벽/낮/노을/밤으로 사용한다.
- 기존 풍경 정책: 겨울 강수는 눈으로 표현한다. 맑은 겨울에는 눈을 내리지 않는다.
  비·겨울에는 나비를, 겨울에는 반딧불이를 띄우지 않는다.
- 설정은 `localStorage["naeru-settings"]`에
  `{season:"auto",band:"auto",motion:"auto",clock:true,date:"en"}`로 저장한다.
  잘못된 값은 무시한다. `motion:"still"`이나 OS 동작 줄이기는 정지 모드다.
- URL 미리보기가 저장된 풍경 설정보다 우선한다. 설정 패널에서 항목을 바꾸면
  그 항목의 URL 강제값만 제거한다. 풍경을 바꿔도 시계는 실제 서울 시각이다.
- 집중 타이머는 `sessionStorage["naeru-focus"]`에 종료 시각을 저장한다.
  새로고침·숨은 탭 이후에도 현재 시각에서 남은 시간을 계산한다.
  완료 시 문구와 `naeru:focus-complete` 이벤트를 보낸다. 가을의 맑은 네 시간대에는
  다가와 인사하고, 다른 풍경에서는 기존 폴짝 인사를 한다. 정지 모드는 문구만
  표시한다. 소리·브라우저 알림 권한을 요청하지 않는다.
  다가오기는 다른 동작이 끝나길 기다리며, 중복 요청은 합친다.
  설정의 `가까이 와줘`와 `naeru:approach` 이벤트로도 시험할 수 있다.
  예약 중에는 호출 버튼을 활성화한다. 버튼이 설정 창을 닫고 기존 예약을
  수동 호출로 합치며, 기다리는 동안 `#approach-wait`에 안내를 표시한다.
  진행 중인 몸짓이 끝나면 `hold`로 다음 몸짓을 막되 영상 재생은 이어간다.
  영상이 뉴트럴 포즈에 도달한 뒤에만 일시 정지하고 접근을 시작한다.
- 일반 접속의 가을 맑은 풍경에서는 처음 한 번, 이후 약 5분마다 다가온다.
  `naeru.isNeutral()`이 busy·hold·표정 기호·몸짓 transform을 확인한다.
  영상도 316프레임 루프의 처음·끝 8프레임에 자연스럽게 도달해야 한다.
  강제 탐색 없이 해당 포즈를 멈추고, 접근·인사·복귀 후 재생을 이어간다.
  첫 방문은 포즈를 먼저 확보한 뒤 마지막 장면 확정의 2.8초 페이드를 기다린다.
  실제 접근 시작부터 다음 5분을 세므로 수동 호출·집중 종료 직후 다시 접근하지 않는다. 탭 복귀·
  장면 전환·동작 재개 때는 새로 5분을 센다. 숨은 탭 방문은 쌓아두지 않는다.
  `act`를 명시한 동작 검사 URL에서는 이 자동 방문을 끈다.

| URL 옵션 | 값·용도 |
|---|---|
| `s` | `spring`, `summer`, `autumn`, `winter` |
| `v` | `dawn`, `day`, `dusk`, `night` |
| `h` | 0~23, 자동 밴드 계산에만 적용 |
| `w` | `clear`, `rain`; 실황 조회를 건너뛰는 미리보기 |
| `still` | `1`: 정지 모드 |
| `ball`, `flit` | `0`: 끄기, `1`: 빠른 동작 검사 |
| `act` | `0`, `hop`, `look`, `flip`, `doze`, `stretch`, `shiver`, `idle`, `approach` |
| `ff` | `0`: 반딧불이 끄기 |
| `tongue` | `1`, `2`: 혀 동작 검사; 기본은 꺼짐 |

`act=approach`는 가을 맑은 네 시간대에서 한 번만 다가오는 미리보기다. 자동 반복하지
않는다. `돌아가기`·내루미 클릭·Escape는 복귀를 시작한다. 탭 숨김·장면 변경·
화면 크기 변경·동작 줄이기는 즉시 원래 자리로 복구한다.

## 자산 작업과 검사

자산은 기존 제작 코드를 통해 고친다. 전체 재생성은 비용이 크므로 수정한
계절·단계만 실행할 수 있다. 배경 전체를 다시 그리면 캐릭터 위치·접지 그림자와
정렬이 깨질 수 있다. 색보정·누끼·인코딩을 건드릴 때는 보존한
[작업 기록](docs/HISTORY-2026-09-08.md)의 해당 함정도 읽는다.

- 가을은 색상 자체를 바꾸고 밝기·붓질은 보존한다. 하늘까지 덥히지 않는다.
- 가을 앞풀은 `tools/foreground/source`의 배경 복원본·투명 원화를 사용한다.
  좌우 제거 영역 밖은 원래 배경을 보존한다. 전경 8장은 동일한 알파를 쓰며
  기존 앞풀의 색 대응으로 조명만 바꾼다. 제작에는 기존 분리용 venv의 OpenCV를 쓴다.
- 겨울 눈은 하늘빛에서 색을 가져온다. 꽃밭은 눈에 묻고, 나무의 밝은 윗면을
  따라 눈을 얹는다. 이미지 경계 이동은 가장자리 값을 연장해 상단 흰 띠를 막는다.
- 강수 하늘은 기존 그림의 대비를 낮춘 잿빛으로 처리하고 지평선에서
  보정 마스크가 갑자기 0이 되지 않게 한다.
- `repaint.py`는 계절 채색·눈 마스크 계산 전에 `ground/repair.py`로 고정
  그림자를 제거한다. 새벽·밤·비의 복원 잔디는 그 변형의 주변 색에 맞춘다.
  원래 `bg-<변형>.jpg`·`plate`는 캐릭터 추출의 입력으로 보존한다.
- VP9는 straight alpha, Safari HEVC는 premultiplied alpha다.
  크롭·루프 프레임 수와 명암 경계를 보존한다.
- `naeru-split/eyes.py`는 생성한 눈 원화를 매 프레임의 눈 윤곽에 맞춰 합성해
  쌍꺼풀처럼 보이는 밝은 띠를 제거한다. 원본 매트는 보존하고 `build/eyes/`에
  인코딩 입력을 만든다. HD와 혀 동작에도 같은 편집을 적용하며 알파는 유지한다.
- 발밑은 복원 배경의 오차가 커서 `|원본-plate|`로 피복률을 계산하지 않는다.
  잔디 색으로 구분한 실루엣을 사용하고 밝고 중성인 발톱만 보완한다.
  상단 피복률과는 `GROUND_FADE`로 연결한다. 임계를 통째로 낮추면 잔디가
  따라온다. [발끝 보정·검증 기록](docs/MATTE-2026-09-11.md)을 참고한다.
- 전체 `build.sh`는 분리·검증·인코딩 뒤 배경 합성·계절 배경·혀 자산까지
  생성하고 판번호를 갱신한다. macOS HEVC 인코더와 별도 LaMa 환경이 필요하다.
- 자산을 바꾼 뒤 `check-assets.py --update-version`으로 내용 해시를 반영한다.
  `--videos`를 더하면 두 코덱의 크기·프레임 수·fps도 검사한다.
- 런타임 변경 후 `tools/browser-check`를 실행한다. 자동 검사 외에
  모바일·밝은 배경·밤 배경을 눈으로 확인한다. Netlify 경로 규칙과
  Safari/iOS 실제 재생은 해당 환경에서 별도 확인한다.

주석은 한국어로 이유와 제약을 설명한다. 일반 화면 변경 때문에 자산 전체를
다시 만들거나 배포 단계를 추가하지 않는다.
