# 밤사이 검증·수리 보고 (2026-09-01)

## BLUF

Sonnet은 02:50에 산출물을 전부 냈고 워처가 03:16에 `DONE`을 냈습니다. 검증해보니
**분리가 실제로는 안 돼 있었습니다.** 겉보기 기준(복원 PSNR 41dB)은 통과했지만,
그건 plate와 스프라이트가 **서로의 오차를 상쇄**해서 통과한 것이었습니다. 제자리에
서 있을 땐 멀쩡하고, **폴짝하는 순간 내루미 모양 얼룩이 드러납니다** — 하필
Phase 1의 대표 동작이 그걸 건드립니다.

원인을 잡아 파이프라인을 고치고 자산을 다시 구웠습니다. JS 버그 5개도 고쳤습니다.
**커밋은 하지 않았습니다** (커밋 = 배포).

---

## 1. 무엇이 잘못돼 있었나

### 축퇴 해 — 두 결함이 서로를 가려주고 있었다

| 어디 | 상태 |
|---|---|
| `plate-*.png` | 지운 자리에 **내루미가 그대로 남아 있었다**(몸통 핵심부) |
| `naeru-*.webm` | 알파에 **배경이 10~50%로 딸려 들어가** 반투명 후광 + 배에 구멍 |
| 합성 결과 | 원본과 41dB로 일치 → **검증 통과** |

`C = P + (O−P)/a` 라서 `P ≈ O`(plate가 캐릭터를 품고 있음)이면 알파가 무엇이든
`O`가 복원됩니다. 제가 계획에 쓴 "복원 PSNR ≥ 40dB" 기준이 이 축퇴 해를 못
걸러낸 것입니다 — **기준 설계가 부실했던 건 제 책임**입니다.

### 근본 원인 둘

1. **매트**: 분홍기 소프트 램프의 하한이 22였는데, 실측하니 배경(잔디·언덕)의
   분홍기가 **p99 = 25~27**입니다. 램프 안에 배경이 통째로 들어갑니다.
   (계획서에 22를 적은 것도 저입니다. `punch.py`는 하드임계 32를 썼는데 그 아래로
   내려간 게 화근이었습니다.)
   - 몸의 하위 5%는 15~18이라 두 분포가 꼬리에서 겹칩니다 → **램프로는 못 가릅니다.**
     이진 임계 30 + 구멍메움 + 연결성분이 맞는 방법입니다.
2. **플레이트**: "시간축에서 가장 안 가려진 프레임 고르기"는 좋은 아이디어지만,
   내루미가 316프레임 내내 같은 자리에 서 있어서 **몸통 핵심부는 시간축 어디에도
   깨끗한 표본이 없습니다.** Sonnet은 "거기는 naeru 레이어가 늘 덮으니 안 보인다"고
   판단했는데(코드 주석에 명시), 제자리에 있을 때만 참입니다.

---

## 2. 고친 것

### 파이프라인
- `matte.py` — 소프트 램프 → **이진 실루엣(임계 30)** + 구멍메움 + **연결성분 필터**
  (`keep_main_component`). 몸 안쪽 시드는 316프레임 실루엣 교집합의 무게중심으로
  실측해서 고정.
- `coords.py` — 키잉창 상단 **1370 → 1394**. 1370~1392에 먼 언덕이 임계를 넘는
  너덜한 가로 띠가 잡히는데 좁은 목으로 머리와 이어져 연결성분으로도 안 떨어집니다.
  머리 돔 꼭대기는 316프레임 전부 y1398~1402이라 1394가 안전합니다.
  (덤으로 언덕에 보이던 사각 이음매도 사라졌습니다 — 패치가 그 띠까지 덮고 있었음)
- `plate.py` — **`inpaint_core()` 신규**. 한 번도 안 드러난 자리를 **같은 행**의
  깨끗한 초원으로 덮고 색 차이만 확산시켜 잇습니다. 같은 y라서 언덕·경계선·풀
  높이가 원래 맞습니다. 패치 마스크에도 연결성분 필터 추가.
- `verify.py` — **GATES 신규**: plate 단독(지운 자리가 아직 캐릭터 색인가) /
  스프라이트 단독(몸 밖 알파 ≤ 3, 몸 안 알파 ≥ 250). 복원 PSNR이 절대 못 잡는 것.

### index.html (JS 버그 5개)
| 버그 | 증상 |
|---|---|
| `schedule()`이 `DEBUG`로 분기 | **두리번이 상시 모드에서 영영 안 돈다.** 폴짝만 두 배로 |
| 공이 `naeru.busy`를 안 봄 | 폴짝 중에 공을 던지면 조준점을 공중에서 읽어 빗나감 |
| `?still=1`이 동작을 안 멈춤 | 픽셀 diff가 비결정적이 됨 |
| 뒤집기를 땅에서 되돌림 | 공중에서만 전환해야 하는데 착지 후 홱 되돌아감 → `facing` 상태로 |
| `headAt()` 폴백이 크롭 기준 | 프레임 기준이어야 함. 공이 엉뚱한 데로 |

### 새 게이트가 실제로 잡아낸 것 (07:35 1차 재빌드)

```
[A] dusk   plate 분홍기 코어=33.8 링=22.4  FAIL — plate에 캐릭터가 남아 있다
[B] 8변형   스프라이트 몸밖 알파=0.0(<=3)  몸안 알파=255.0(>=250)  전부 PASS
```

스프라이트는 8변형 전부 깨끗해졌고(몸 밖 알파 0.0, 몸 안 255.0 — 후광도 구멍도
없음), plate는 dusk만 걸렸습니다. 원인은 캐릭터 잔상이 아니라 **도너 색 보정의
확산 반경이 부족**해서 언덕 한복판에 도너의 따뜻한 색이 남은 것이었습니다.
반경을 28→96에서 시작하도록 고치니 코어 20.1 / 링 22.2로 맞았습니다.
게이트가 인코딩 **전에** 멈춰 세워서 헛돈 시간이 없었습니다.

### 문서
- `CLAUDE.md` — 배경 영상이 "내루미가 지워진 채로 구워져 있다"는 **사실과 다른
  서술**을 정정(영상은 안 건드렸고 런타임 패치로 지웁니다). 함정 4개 추가.
- `PLAN-naeru-split.md` — §3.8에 게이트를 0번 항목으로 추가.

---

## 3. 최종 검증 결과 (08:2x, 전부 통과)

```
[GATES] plate 분홍기 코어 vs 링 — 8변형 전부 PASS (dusk 20.1/22.2, day 0.0/0.4 …)
        스프라이트 몸밖 알파 0.0(<=3) · 몸안 알파 255.0(>=250) — 8변형 전부 PASS
[PASS1 PNG시퀀스]  n=2528  avg=40.79dB  worst=35.80dB  PASS
[PASS2 webm디코딩] n=2528  avg=39.56dB  worst=35.34dB  PASS (기준 38.5, 근거는 verify.py 주석)
```

- **브라우저 6화면비 × 4변형 픽셀 대조**(기존 배포본 vs 신규, reduced-motion 강제):
  24건 전부 평균차 0.06~0.40 / 255. 세로 모바일(390×844)만 p99가 9까지 올라가는데,
  프레임이 2.7배로 확대되는 화면비라 실루엣 경계의 안티에일리어싱 차이가 커진 것이고
  **위치 어긋남은 없습니다**(어긋났다면 한쪽으로 쏠린 초승달 모양 차이가 나옵니다).
- `img/meadow-*`·`img/sky-*`·`og.jpg` **변경 0건** — 배경 자산은 안 건드렸습니다.
- 프레임 수 316 유지(webm/mp4 모두 실측).
- 신규 자산 **23MB** (Sonnet 판 37MB에서 줄었습니다 — 알파가 깨끗해져 더 잘 압축됩니다).

## 4. 남은 것 / 판단이 필요한 것

- **인페인트 품질**: 폴짝 정점에서 몸 아래가 잠깐 보이는데, 그 자리는 합성된
  초원입니다. 내루미 모양 얼룩은 없앴지만 도너에서 딸려온 **풀숲 한 덩이**가
  들어와 있습니다. ~400ms 동안 몸 뒤로 보이는 자리라 넘어갈 만하다고 판단했는데,
  눈으로 보시고 아니다 싶으면 `plate.py`의 `DONOR_DX`(현재 1024)를 바꾸면 다른
  자리에서 빌려옵니다.
- **PSNR 기준을 제가 낮췄습니다**(pass2 40 → 38.5). 근거는 `verify.py` 주석에
  적어뒀습니다 — 40dB는 축퇴 해를 재던 값이고, 제대로 분리하면 진짜 컷아웃이라
  복원값이 내려갑니다(상한이 pass1 41.0dB, crf를 34→30으로 올려도 +0.2dB뿐).
  기준을 낮춘 건 제 판단이니 동의 안 되시면 되돌리세요.
- **발·그림자**: 접지 그림자는 배경에 남겨두는 설계라 발도 같이 남습니다.
  폴짝 높이(13%)에서는 큰 문제가 아니지만, Phase 2로 가면 그림자 분리가 필요합니다.
- **아이폰 Safari 실기 확인**: 자동화가 안 되는 항목이라 그대로 남아 있습니다.
  HEVC 알파가 투명하게 재생되는지, 동시 디코더 4장이 버티는지.
- **용량**: 신규 자산 23MB(계획서 예상 5~10MB보다는 큼). 더 줄이려면 webm `-crf`와
  HEVC `-q:v`를 낮추면 되는데, 화질 대비 이득이 크지 않아 그대로 뒀습니다.
- **아침에 눈으로 볼 것 하나**: `?act=hop`으로 폴짝을 반복 재생시켜 몸 아래가
  드러나는 순간이 자연스러운지. 제가 정지 프레임 합성으로는 확인했지만
  움직이는 걸 실제로 보진 못했습니다.
